# mouseum-esp32 partition table
#
# Sized for a 4 MB+ flash (every supported dev kit ships with at least
# that).  The 2 MB factory slot fits the larger BLE/NimBLE build on the C6
# with comfortable head-room; the S3 USB-only build uses about 1.3 MB.
#
# Name,    Type, SubType, Offset,  Size,    Flags
nvs,       data, nvs,     0x9000,  0x6000,
phy_init,  data, phy,     0xf000,  0x1000,
factory,   app,  factory, 0x10000, 0x200000,
